@{
  Layout = "_Layout";
}

@model HairSalon.Models.Appointment

<div class="mainBox">
  <h2>Are you sure you want to delete appointment <strong>@Html.DisplayFor(model => model.Title)</strong>?</h2>
  <hr />  
  @using (Html.BeginForm())
  {
      <input type="submit" value="Delete!" class="delete" />
  }
  @Html.ActionLink("Back to appointments", "Index",null,null, new { @class = "btn btn-info btn-large" })
</div>