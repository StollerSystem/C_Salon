@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Client>;

<h2>Clients</h2>

@if (@Model.Count == 0)
{
  <h3>No Clients have been added yet!</h3>
} 

@foreach (Client client in Model)
{
  <li>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId }) | @client.Stylist.Name</li>
}

<p>@Html.ActionLink("Add new Client", "Create", null, null, new { @class = "btn btn-info btn-large" })</p>

<p>@Html.ActionLink("Home", "Index", "Home", null, new { @class = "btn btn-info btn-large" })</p>
